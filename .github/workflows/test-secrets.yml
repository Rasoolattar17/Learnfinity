name: Test Secrets Configuration

on:
  workflow_dispatch:

jobs:
  test-secrets:
    runs-on: ubuntu-latest
    steps:
      - name: Test Secret Configuration
        run: |
          echo "🔍 Testing GitHub Secrets configuration..."
          
          # Test if secrets are configured
          if [ -n "${{ secrets.SERVER_HOST }}" ]; then
            echo "✅ SERVER_HOST is configured"
          else
            echo "❌ SERVER_HOST is NOT configured"
          fi
          
          if [ -n "${{ secrets.SERVER_USERNAME }}" ]; then
            echo "✅ SERVER_USERNAME is configured"
          else
            echo "❌ SERVER_USERNAME is NOT configured"
          fi
          
          if [ -n "${{ secrets.SERVER_SSH_KEY }}" ]; then
            echo "✅ SERVER_SSH_KEY is configured"
          else
            echo "❌ SERVER_SSH_KEY is NOT configured"
          fi
          
          if [ -n "${{ secrets.SERVER_BASE_PATH }}" ]; then
            echo "✅ SERVER_BASE_PATH is configured"
            echo "   Value: ${{ secrets.SERVER_BASE_PATH }}"
          else
            echo "❌ SERVER_BASE_PATH is NOT configured"
          fi
          
          if [ -n "${{ secrets.FIRST_TIME_DEPLOYMENT }}" ]; then
            echo "✅ FIRST_TIME_DEPLOYMENT is configured"
            echo "   Value: ${{ secrets.FIRST_TIME_DEPLOYMENT }}"
          else
            echo "❌ FIRST_TIME_DEPLOYMENT is NOT configured"
          fi
          
          echo ""
          echo "📋 Summary:"
          echo "If you see any ❌ above, you need to configure those secrets in GitHub."
          echo "Go to: Settings → Secrets and variables → Actions" 